import axios from "axios";
import React, { useEffect } from "react";

const Timer = (phone) => {
   
    return (
        <div id="currentTextTimer" >
            <span className="font-bold" id="timerrrrrrrrrrrrrrrrrrrrrrr">
            </span>
            <span> تا انقضا و درخواست مجدد کد </span>
        </div>
    );
};

export default Timer;
